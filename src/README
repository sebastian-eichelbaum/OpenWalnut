See the "doc" directory for more information, e.g. on build and installation process.

------------------------------------------------------

Directory Structure:
    -gui
    -dataHandler
    -graphicsEngine
    -common
    -kernel
    -tools
